const mobileMaxWidth = "(max-width: 870px)";
export default {
  MOBILE: `@media ${mobileMaxWidth}`,
};

// export let IS_MOBILE = false;

// function isMobile() {
//   // TODO: Throttle
//   IS_MOBILE = window.matchMedia(mobileMaxWidth).matches;
// }

// window.addEventListener('resize', isMobile);
